# Tests

SSRS currently has no executable tests.

Use documentation review as described in:

```text
docs/validation.md
```

If executable report tooling is added later, add the appropriate validation runner before treating the project as implementation-ready.
